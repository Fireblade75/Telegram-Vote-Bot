{
  "name": "vote-bot",
  "version": "1.0.0",
  "description": "A telegram vote bot",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "tsc && node dist/index.js "
  },
  "keywords": [
    "telegram",
    "vote"
  ],
  "author": "Fireblade75",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "telegraf": "^3.33.3"
  }
}
